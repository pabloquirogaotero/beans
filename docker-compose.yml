services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: beans
      POSTGRES_USER: beans
      POSTGRES_PASSWORD: beans
    ports:
      - "5432:5432"
    volumes:
      - beans-data:/var/lib/postgresql/data
volumes:
  beans-data:
